enum Details {
    User = 'Bharath',
    SuperUser = 'Raja',
    Admin = 'Masai',
    SuperAdmin = 'Masai School',
  }
  
  let User = Details.User; 
  let SuperUser = Details.SuperUser;
  let Admin = Details.Admin;
  let SuperAdmin = Details.SuperAdmin
  console.log(User);
  console.log(SuperUser);
  console.log(Admin);
  console.log(SuperAdmin); 